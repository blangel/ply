# overrides the build.path for the test scope; note ${project.build.dir} will be resolved in the test scope as well
build.path=${project.build.dir}/test-classes
# overrides the class.deps for the test scope; note ${project.build.dir} will be resolved in the test scope as well
class.deps=${project.build.dir}/test-classdeps