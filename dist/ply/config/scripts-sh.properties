# The shell to be used for unix commands (i.e., those surrounded by tick marks '`') or those ending in '.sh'
shell=/bin/bash
# The default arguments to be passed to the shell whenever a unix command is run
shell.args=-c
